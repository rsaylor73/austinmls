{% extends 'base.html.twig' %}
{% block header %}
	{% include('header.html.twig') %}
{% endblock %}

{% block body %}

	{% include('flash-messages.html.twig') %}

	<div class="row top-buffer">
		<div class="col-sm-12">
			<h3>Manage Saved Search</h3>
		</div>
	</div>
  
	<div class="row top-buffer">
		<div class="col-sm-12">
			<table class="table table-striped" id="myTable">
				<thead>
					<th>User</th>
					<th>Date Added</th>
					<th>Search Link</th>
					<th>Actions</th>
				</thead>
				<tbody>
				{% for d in data %}
				<tr>
					<td><a href="mailto:{{d.email}}">{{d.first}} {{d.last}}</a></td>
					<td>{{d.date}}</td>
					<td><a href="{{ path('list-map') }}?s=1{{d.link}}">View Search</a></td>
					<td>
						<input type="text" value="Delete" class="btn btn-warning" onclick="if(confirm('You are about to delete this search. Click OK to continue.')) { document.location.href=' {{path('admindeletesearch', {'id': d.id } )}}   '}">
					</td>
				</tr>
				{% endfor %}
				</tbody>
			</table>
		</div>
	</div>


<br><br><br><br><br><br>

<script>
$(document).ready(function(){
    $('#myTable').DataTable({
      "aaSorting": []
    });
});
</script>


{% endblock %}

{% block footer %}
	{% include('footer.html.twig') %}
{% endblock %}